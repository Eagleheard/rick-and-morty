import React from "react";

import { Button } from "../Button";

import "./styles.scss";

export const Header = () => {
  return (
    <header className="header">
      <Button />
    </header>
  );
};
